emit:
    merge:
        emit vars:
            get a PatternTuple type
            use test.py to debug easily

    add depth function

    split up MatchParam?

    try to debug update/remove:
        upgrade Python first?

    ANNOUNCE:
        possibly clean up dirs first?

    import cleanup:
        (need to get actual import working)
        move GT to Basics? or just do prelude to match Elm semantically?

    keep trying to simplify pattern parsing

    cleanup:
        add annotations
        yapf

