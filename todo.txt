emit:

    pratt:
        start with simple test case
        don't worry about embedding

        every function:
            return led, nud, state

    import cleanup:
        (need to get actual import working)
        move GT to Basics? or just do prelude to match Elm semantically?

